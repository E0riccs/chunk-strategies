现在，我想要针对 RAG 应用中的 chunk 切片功能进行探究。对于不同的“文件类型+切片策略”组合，我要不断尝试以获得最优解决方案。
1. 文件类型：章节类型原始长文本、规则类型分点短文本、无分割短文本等，要求能够后续自定义添加策略，并且指向一个对应此类型的供测试数据文件；
2. 切片策略: 包括简单分割、递归分割等方法, 并且每一种方法中都有一些具体参数，即使方法名字相同而参数不同，也认为是不同的切片策略。
3. 评价指标：总体运行时间、使用特定Prompt调制好的大模型（评价）输出的评价结果（5分制）、切片结果和原始文本的余弦相似度整合。
4. 输入：要求能够分别制定文件类型和切片策略；
5. 输出：针对不同的文件类型和切片策略，输出并保存切片结果至结果文件（每次试验均保存至不同文件），输出并保存评价指标至一个评价表格（每次试验均保存至不同文件）
6. 运行策略：文件类型和切片策略应该高度抽象化，人工在每次尝试试验的时候只需要引用文件类型名称和切片策略名称，最终结果的展现则需要展现“文件类型名称”和“切片策略中的方法名称+参数”

整体要求：1. 由于这是个测试流程项目，因此其中各个部分要做好解耦分离。 2. 所有的大模型均通过api形式调用。 3. 文件类型和切片策略的定义应该通过yaml结构化的定义


现在，我要详细的使用大模型来对分片结果进行评价
1. 应该模仿已有工程的结构，将大模型的调用封装成一个类 llm_evaler；
1. 他应该通过config文件夹内的文件来读取相应的大模型参数，prompt；
2. 首先，使用大模型（large）针对原文生成QA对，并储存至本地 filetype.txt 文件中；
2. 使用大模型（small）对分片结果，进行reranker后，回答Qs；
3. 计算QA对的召回率；/ 使用大模型（large）比较Q-A-A，给出主观评分；